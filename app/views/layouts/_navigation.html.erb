<%= link_to "Classroom Economy", root_path, :class => 'brand' %>
<ul class="nav">
  <% if user_signed_in? %>
    <li>
      <%= current_user.display_name %>
    </li>
    <% if current_user.teacher? || current_user.student? %> <%# && current_user.groups.count > 1 %>
      <li>
        Select Class:
        <%= select_tag :class, options_from_collection_for_select(current_user.groups, "id", "name", get_current_group_id) %>
      </li>
    <% end %>
    <li>
    <%= link_to('Logout', destroy_user_session_path, :method=>'delete') %>
    </li>
  <% else %>
    <li>
    <%= link_to('Login', new_user_session_path)  %>
    </li>
  <% end %>
  <% if user_signed_in? %>
    <li>
    <%= link_to('Edit account', edit_user_registration_path) %>
    </li>
    <% if current_user.admin? %>
      <li>
      <%= link_to('Admin', users_path) %>
      </li>
    <% end %>
  <% else %>
    <li>
    <%= link_to('Sign up', new_user_registration_path)  %>
    </li>
  <% end %>
  <li>
    <%= link_to("A", switch_user_path(user_id: 2)) %>
  </li>
  <li>
    <%= link_to("T", switch_user_path(user_id: 1)) %>
  </li>
  <li>
    <%= link_to("S", switch_user_path(user_id: 3)) %>
  </li>


</ul>