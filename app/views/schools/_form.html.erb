<%= form_for(@school) do |f| %>

  <%= render partial: "/shared/errors", locals: { o: @job } %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
